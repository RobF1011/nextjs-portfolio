import { Roboto, Orbitron } from 'next/font/google';

export const roboto = Roboto({ weight: ['400', '700'], subsets: ['latin'] });

export const orbitron = Orbitron({ weight: ['400', '700'], subsets: ['latin'] });